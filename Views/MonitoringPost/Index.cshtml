@model IEnumerable<JwtAuthApp.Models.MonitoringPost>

@{
    ViewData["Title"] = "Посты мониторинга";
}

<h2>Посты мониторинга</h2>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<p>
    <a asp-action="Create" class="btn btn-primary">Создать пост</a>
</p>

<table class="table table-striped table-hover">
    <thead>
        <tr>
            <th>Название</th>
            <th>Описание</th>
            <th>Координаты</th>
            <th>Мобильный</th>
            <th>Статус</th>
            <th>Создан</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="@(item.IsActive ? "" : "table-secondary")">
                <td>@item.Name</td>
                <td>@(item.Description ?? "—")</td>
                <td>
                    @if (item.Latitude.HasValue && item.Longitude.HasValue)
                    {
                        <span>@item.Latitude°, @item.Longitude°</span>
                    }
                    else
                    {
                        <span class="text-muted">Нет координат</span>
                    }
                </td>
                <td>
                    @if (item.IsMobile)
                    {
                        <span class="badge bg-info">Мобильный</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">Стационарный</span>
                    }
                </td>
                <td>
                    @if (item.IsActive)
                    {
                        <span class="badge bg-success">Активен</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Неактивен</span>
                    }
                </td>
                <td>@item.CreatedAt.ToString("dd.MM.yyyy HH:mm")</td>
                <td>
                    <div class="btn-group" role="group">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Подробнее</a>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Редактировать</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Удалить</a>
                        <form asp-action="ToggleActive" asp-route-id="@item.Id" method="post" style="display: inline;">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-sm @(item.IsActive ? "btn-secondary" : "btn-success")">
                                @(item.IsActive ? "Деактивировать" : "Активировать")
                            </button>
                        </form>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>
